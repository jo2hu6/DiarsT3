@model List<DiarsT3.Models.Rutina>

<h3>Mis rutinas</h3>

<div class="row mb-3">
    <div class="col">
        <a href="/Rutina/Registrar" class="btn btn-success">Registrar rutina</a>
    </div>
</div>

<div class="row">
    <div class="col-12">
        <table class="table">
            <thead>
                <tr>
                    <th>Rutina</th>
                    <th>Duración</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var rutina in Model)
                {
    <tr>
        <td>@rutina.Nombre </td>
        <td>@rutina.CalcularDuracion() segundos </td>
        <td>
            <a href="/Rutina/Detalle/@rutina.Id" class="btn btn-primary">Detalle</a>
        </td>
    </tr>}
            </tbody>
        </table>
    </div>
</div>